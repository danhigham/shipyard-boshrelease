set -e # exit immediately if a simple command exits with a non-zero status
set -u # report the usage of uninitialized variables

# Copy Shipyard Cluster Helpers package
echo "Copying Shipyard Cluster Helpers..."
cp -a ${BOSH_COMPILE_TARGET}/shipyard-cluster-helpers/* ${BOSH_INSTALL_TARGET}/
